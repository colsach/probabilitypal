import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dist-vis-final',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
