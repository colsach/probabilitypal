import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.colsach.propabilitypal',
  appName: 'ProbabilityPal',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
